node_modules
out
dist
.turbo
apps/*/.turbo
apps/web/dist
# Staging/retired trees and the lock from tooling/scripts/sync-web-dist.mjs.
# The bare `dist` above does not cover these suffixed names.
dist.stage-*
dist.retired-*
apps/server/web/.web-dist.lock
apps/server/bin
.DS_Store
*.log
.env
.env.*
!.env.example
.vscode/*
!.vscode/extensions.json
.idea
.claude/
*.tsbuildinfo
data/
/vault/
/remotevault/

# Local spec-driven-dev methodology (personal fork tooling — not for upstream)
/CLAUDE.md
/context/

# Local commitcraft config (personal tooling — not for upstream)
/.commitcraft.toml

# Nix build outputs
result
result-*
# Release notes / launch copy are kept local only, never committed.
docs/releases/
